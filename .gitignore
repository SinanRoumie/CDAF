.venv/
__pycache__/
*.pyc
.DS_Store
# Validation / training run outputs -- diagnostic artifacts, never committed
# (checkpoints, rounds, metrics, resolved configs). Flagged in the Run 2 handoff.
runs/
