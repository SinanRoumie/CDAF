"""CDAF post-hoc transcript renderer (docs/render_spec.md).

An inspection and publication artifact. NOT part of the environment, the reward,
or any training loop (RS1-RS3). Read-only with respect to everything else in the
tree. M0 is a template renderer with no LLM calls (spec §8).
"""
